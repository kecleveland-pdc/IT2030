@model IEnumerable<EventFinder.Models.EventFinderEventType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EventType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EventType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventFinderEventTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventFinderEventTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventFinderEventTypeID })
        </td>
    </tr>
}

</table>
